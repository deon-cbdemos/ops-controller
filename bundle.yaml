apiVersion: "1"
version: "1"
id: "deon-cbdemos-ops-controller"
description: "CloudBees CI configuration bundle for the deon-cbdemos ops-controller Controller"
jcasc:
  - "jenkins.yaml"
plugins:
  - "plugins.yaml"
